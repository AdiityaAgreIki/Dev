#!/usr/bin/env python3
"""
Diet Server Application

A FastAPI application for extracting data from body composition analysis reports.
"""

import uvicorn
from app.main import app

if __name__ == "__main__":
    uvicorn.run(
        "app.main:app",
        host="0.0.0.0",
        port=8000,
        reload=True,
        log_level="info"
    )
